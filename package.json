{
  "name": "fallensmp",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "mkdir -p public && cp index.html public/"
  },
  "dependencies": {
    "@hapi/iron": "^7.0.0",
    "cookie": "^0.5.0",
    "passport": "^0.6.0",
    "passport-discord": "^0.1.4"
  }
}
